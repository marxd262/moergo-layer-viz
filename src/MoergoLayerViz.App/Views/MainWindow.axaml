<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:MoergoLayerViz.App.ViewModels"
        xmlns:views="using:MoergoLayerViz.App.Views"
        xmlns:conv="using:MoergoLayerViz.App.Converters"
        x:Class="MoergoLayerViz.App.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        x:Name="MainWin"
        Title="{Binding [Title_App], Source={StaticResource Loc}}"
        Width="1440" Height="680"
        MinWidth="640" MinHeight="320"
        TransparencyLevelHint="Transparent,AcrylicBlur,None"
        Background="Transparent"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaChromeHints="NoChrome"
        ExtendClientAreaTitleBarHeightHint="0"
        SystemDecorations="None"
        Topmost="{Binding IsAlwaysOnTop}">

    <Window.Resources>
        <conv:HexColorToBrushConverter x:Key="HexToBrush" />
    </Window.Resources>

    <Window.Styles>
        <!-- Shared toggle-button style: dim when off, fully opaque when .active. -->
        <Style Selector="Button.toggle">
            <Setter Property="Opacity" Value="0.4" />
        </Style>
        <Style Selector="Button.toggle.active">
            <Setter Property="Opacity" Value="1.0" />
        </Style>
        <!-- Layer-tab override: text labels on #2A2A3D need more contrast than icons
             in the status bar. Inactive stays readable (0.65); active gets a
             distinct raised-chip look via background + brighter border. -->
        <Style Selector="Button.toggle.layerTab">
            <Setter Property="Opacity" Value="0.65" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
        </Style>
        <Style Selector="Button.toggle.layerTab:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#363649" />
            <Setter Property="BorderBrush" Value="#4A4A63" />
        </Style>
        <Style Selector="Button.toggle.layerTab.active">
            <Setter Property="Opacity" Value="1.0" />
        </Style>
        <Style Selector="Button.toggle.layerTab.active /template/ ContentPresenter">
            <Setter Property="Background" Value="#3E3E5C" />
            <Setter Property="BorderBrush" Value="#6A6A88" />
        </Style>
        <Style Selector="Button.toggle.layerTab.active:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#46466A" />
            <Setter Property="BorderBrush" Value="#7A7A9A" />
        </Style>
    </Window.Styles>

    <Grid RowDefinitions="Auto,*,Auto">

        <!-- Bars panel: Row 0 (top) or Row 2 (bottom), switched automatically.
             Inner Grid swaps StatusBar/LayerTabs so the status bar is always at the outer edge. -->
        <Grid x:Name="BarsPanel" Grid.Row="0" RowDefinitions="Auto,Auto">

            <!-- Status bar (solid, also serves as drag handle) -->
            <Border x:Name="StatusBar" Grid.Row="0" Background="#1E1E2E" Padding="12,8"
                    CornerRadius="8,8,0,0">
                <DockPanel>
                    <Button x:Name="QuitButton" DockPanel.Dock="Left"
                            Command="{Binding QuitCommand}"
                            Focusable="False"
                            Padding="6,4" Margin="0,0,4,0"
                            VerticalAlignment="Center"
                            ToolTip.Tip="{Binding [Tooltip_Quit], Source={StaticResource Loc}}">
                        <PathIcon Width="12" Height="12"
                                  Data="M18.3 5.71a1 1 0 0 0-1.41 0L12 10.59 7.11 5.7A1 1 0 0 0 5.7 7.11L10.59 12 5.7 16.89a1 1 0 1 0 1.41 1.41L12 13.41l4.89 4.89a1 1 0 0 0 1.41-1.41L13.41 12l4.89-4.89a1 1 0 0 0 0-1.4z" />
                    </Button>
                    <Button x:Name="MinimizeButton"
                            DockPanel.Dock="Left"
                            Focusable="False"
                            Padding="6,4" Margin="0,0,8,0"
                            VerticalAlignment="Center"
                            ToolTip.Tip="{Binding [Tooltip_Minimize], Source={StaticResource Loc}}">
                        <PathIcon Width="12" Height="12"
                                  Data="M20 14H4v-2h16v2z" />
                    </Button>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                                HorizontalAlignment="Right" Spacing="4">
                        <StackPanel.Styles>
                            <Style Selector="StackPanel > Button">
                                <Setter Property="Focusable" Value="False" />
                            </Style>
                        </StackPanel.Styles>

                        <!-- Keyboard profile picker — switches the physical layout (GO60 / Glove80). -->
                        <Button x:Name="KeyboardPickerButton"
                                Padding="8,4"
                                ToolTip.Tip="{Binding [Tooltip_Keyboard], Source={StaticResource Loc}}">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <PathIcon Width="14" Height="14"
                                          Data="M20 5H4c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm-9 3h2v2h-2V8zm0 3h2v2h-2v-2zM8 8h2v2H8V8zm0 3h2v2H8v-2zm-1 2H5v-2h2v2zm0-3H5V8h2v2zm9 7H8v-2h8v2zm0-4h-2v-2h2v2zm0-3h-2V8h2v2zm3 3h-2v-2h2v2zm0-3h-2V8h2v2z" />
                                <TextBlock Text="{Binding SelectedKeyboard.DisplayName}"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <Button.Flyout>
                                <Flyout Placement="Bottom">
                                    <ItemsControl ItemsSource="{Binding AvailableKeyboards}" Padding="2">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Command="{Binding DataContext.SelectKeyboardCommand, ElementName=MainWin}"
                                                        CommandParameter="{Binding}"
                                                        Padding="10,6"
                                                        HorizontalAlignment="Stretch"
                                                        HorizontalContentAlignment="Left"
                                                        Background="Transparent">
                                                    <Grid ColumnDefinitions="16,*">
                                                        <TextBlock Grid.Column="0"
                                                                   Text="✓"
                                                                   VerticalAlignment="Center"
                                                                   FontWeight="Bold">
                                                            <TextBlock.IsVisible>
                                                                <MultiBinding Converter="{x:Static conv:EqualityToBoolMultiConverter.Instance}">
                                                                    <Binding Path="Id" />
                                                                    <Binding Path="DataContext.SelectedKeyboard.Id" ElementName="MainWin" />
                                                                </MultiBinding>
                                                            </TextBlock.IsVisible>
                                                        </TextBlock>
                                                        <TextBlock Grid.Column="1"
                                                                   VerticalAlignment="Center">
                                                            <Run Text="{Binding DisplayName, Mode=OneWay}" />
                                                            <Run Text=" — " Foreground="#8A8AA0" />
                                                            <Run Text="{Binding KeyCount, Mode=OneWay}" Foreground="#8A8AA0" />
                                                            <Run Text=" keys" Foreground="#8A8AA0" />
                                                        </TextBlock>
                                                    </Grid>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Flyout>
                            </Button.Flyout>
                        </Button>

                        <!-- Live key highlighting (SharpHook-driven pressed-key glow) -->
                        <Button x:Name="LiveButton"
                                Command="{Binding ToggleLiveHighlightingCommand}"
                                Classes="toggle"
                                Classes.active="{Binding IsLiveHighlightingEnabled}"
                                Padding="8,4"
                                ToolTip.Tip="{Binding [Tooltip_LiveHighlighting], Source={StaticResource Loc}}">
                            <PathIcon Width="16" Height="16"
                                      Data="M3 12h4l3-8 4 16 3-8h4" />
                        </Button>

                        <!-- Auto layer switch — follows signal-macro keys -->
                        <Button x:Name="AutoLayerButton"
                                Command="{Binding ToggleAutoLayerSwitchCommand}"
                                Classes="toggle"
                                Classes.active="{Binding IsAutoLayerSwitchEnabled}"
                                Padding="8,4"
                                IsVisible="{Binding IsLiveHighlightingEnabled}"
                                ToolTip.Tip="{Binding [Tooltip_AutoLayerSwitch], Source={StaticResource Loc}}">
                            <PathIcon Width="14" Height="14"
                                      Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" />
                        </Button>

                        <!-- Reset active layer to base -->
                        <Button x:Name="ResetButton" Command="{Binding ResetLayerStateCommand}"
                                Padding="8,4"
                                IsVisible="{Binding IsAutoLayerSwitchEnabled}"
                                ToolTip.Tip="{Binding [Tooltip_ResetLayerTracking], Source={StaticResource Loc}}">
                            <PathIcon Width="14" Height="14"
                                      Data="M12 5V1L7 6l5 5V7c3.31 0 6 2.69 6 6s-2.69 6-6 6-6-2.69-6-6H4c0 4.42 3.58 8 8 8s8-3.58 8-8-3.58-8-8-8z" />
                        </Button>

                        <!-- Load layout JSON -->
                        <Button x:Name="LoadLayoutButton"
                                Command="{Binding LoadLayoutCommand}"
                                Padding="8,4"
                                ToolTip.Tip="{Binding [Tooltip_LoadLayout], Source={StaticResource Loc}}">
                            <PathIcon Width="14" Height="14"
                                      Data="M10 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V8c0-1.1-.9-2-2-2h-8l-2-2z" />
                        </Button>

                        <!-- Refresh loaded JSON from disk -->
                        <Button x:Name="RefreshButton" Command="{Binding RefreshCommand}"
                                Padding="8,4"
                                IsVisible="{Binding HasLayoutLoaded}"
                                ToolTip.Tip="{Binding [Tooltip_Refresh], Source={StaticResource Loc}}">
                            <PathIcon Width="16" Height="16"
                                      Data="M18.537 19.567A9.961 9.961 0 0 1 12 22C6.477 22 2 17.523 2 12S6.477 2 12 2s10 4.477 10 10c0 2.136-.67 4.116-1.81 5.74L17 12h3a8 8 0 1 0-2.46 5.77z" />
                        </Button>

                        <!-- Pin (always on top) -->
                        <Button x:Name="PinButton"
                                Command="{Binding TogglePinCommand}"
                                Classes="toggle"
                                Classes.active="{Binding IsAlwaysOnTop}"
                                Padding="8,4"
                                ToolTip.Tip="{Binding [Tooltip_AlwaysOnTop], Source={StaticResource Loc}}">
                            <PathIcon Width="16" Height="16"
                                      Data="M16 12V4h1V2H7v2h1v8l-2 2v2h5.2v6h1.6v-6H18v-2z" />
                        </Button>

                        <!-- Open log folder (diagnostic) -->
                        <Button x:Name="OpenLogFolderButton" Command="{Binding OpenLogFolderCommand}"
                                Padding="8,4"
                                ToolTip.Tip="{Binding [Tooltip_OpenLogFolder], Source={StaticResource Loc}}">
                            <PathIcon Width="14" Height="14"
                                      Data="M10 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V8c0-1.1-.9-2-2-2h-8l-2-2z" />
                        </Button>

                        <!-- Copy diagnostics -->
                        <Button x:Name="CopyDiagnosticsButton" Command="{Binding CopyDiagnosticsCommand}"
                                Padding="8,4"
                                ToolTip.Tip="{Binding [Tooltip_CopyDiagnostics], Source={StaticResource Loc}}">
                            <PathIcon Width="14" Height="14"
                                      Data="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z" />
                        </Button>
                    </StackPanel>
                    <!-- Fill child: takes remaining space, truncates with ellipsis.
                         Status message + dim keyboard-profile hint appended on the right. -->
                    <TextBlock VerticalAlignment="Center"
                               MinWidth="20"
                               TextTrimming="CharacterEllipsis">
                        <Run Text="{Binding StatusMessage}" Foreground="#CDD6F4" />
                        <Run Text="  ·  " Foreground="#6A6A88" />
                        <Run Text="{Binding KeyboardStatusHint}" Foreground="#8A8AA0" />
                    </TextBlock>
                </DockPanel>
            </Border>

            <!-- Layer tabs -->
            <Border x:Name="LayerTabs" Grid.Row="1"
                    Background="#2A2A3D"
                    Padding="8,2">
                <ItemsControl ItemsSource="{Binding Layers}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:LayerViewModel">
                            <Button Command="{Binding SelectCommand}"
                                    MinWidth="48"
                                    Classes="toggle layerTab"
                                    Classes.active="{Binding IsSelected}"
                                    HorizontalContentAlignment="Center"
                                    Padding="8,4">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <Border Width="10" Height="10" CornerRadius="5"
                                            Background="{Binding TabColor, Converter={StaticResource HexToBrush}}" />
                                    <TextBlock Text="{Binding DisplayName}"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </Grid>

        <!-- Board visualization area -->
        <Border Grid.Row="1" Padding="16"
                Background="#14141F">
            <views:BoardView DataContext="{Binding}" />
        </Border>

    </Grid>

</Window>
